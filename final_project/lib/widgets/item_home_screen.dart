import 'package:flutter/material.dart';
class Item_home_screen extends StatelessWidget {
  String item_name;
  Color color_card;
   Item_home_screen(this.item_name,this.color_card);

  @override
  Widget build(BuildContext context) {
      double height_mob = MediaQuery. of(context). size. height;
    return Scaffold(
   
     
    );
  }
} 